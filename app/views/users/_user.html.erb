<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if check_user?(user) %>
    | <%= link_to t("user.delete"), user, method: :delete,
      data: { confirm: (t "user.alert") } %>
  <% end %>
</li>
